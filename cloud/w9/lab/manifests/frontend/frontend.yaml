apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: w9-platform
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    app: backend
