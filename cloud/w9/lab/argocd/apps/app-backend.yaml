apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-backend
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/PTienhocSE/ptien-aws-accelerator-p2.git'
    targetRevision: HEAD
    path: cloud/w9/lab/manifests/backend
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: w9-platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
